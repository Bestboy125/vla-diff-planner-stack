# 验证记录（2026-09-05）

- 6 项 unittest 全部通过：B 相机原点/坐标轴、相机旋转和不同内参、负深度、无效或零基线、非法输入、已知对应点全估计流程。
- 包内录制图像在 CPU 上完整推理成功。测试显式将 `torch.hub.load_state_dict_from_url` 替换为报错函数，确认不需要下载模型。
- 固定相同 OpenCV 随机种子，原估计器与独立包分别使用相同的特征引擎，在 CPU 和 CUDA 各自对比。输出三维坐标完全一致（检查绝对容差 1e-9 m）。对比覆盖原匹配器和原估计器；模型仍来自原项目同一份网络和权重。
- CUDA 结果与原录制结果一致：[-121.213124, -21.830613, 305.081060] m；68 个内点，重投影误差 0.274021 px。
- CPU 结果：[-119.306729, -25.608121, 308.393852] m；72 个内点，重投影误差 0.262669 px。此样本 CPU/CUDA 的目标坐标相差约 5.374 m，原实现也存在该差异；跨设备不能假定结果逐位一致。

上述结果验证代码抽取一致性和离线可运行性，不是目标绝对位置精度或真机性能验证。CPU 单次完整运行约 6.7 秒，仅代表这台开发机、这一样本，不能推断目标设备实时帧率。

详细数值保存在 `examples/recorded/parity_report.json`。权重和当前核心均与源文件哈希一起记录在 `SOURCE_MANIFEST.json`。
